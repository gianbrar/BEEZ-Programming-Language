#!/bin/bash

if [ $EUID -ne 0 ]
  then echo 'Please run as sudo.'
  exit
fi

cd src
cd b++
g++ -o b++ main.cpp
mv b++ /usr/bin/
alias b++=/usr/bin/b++
echo "alias b++=/usr/bin/b++" >> ~/.bashrc
echo "Install successful!"
